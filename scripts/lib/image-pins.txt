# scripts/lib/image-pins.txt -- RFC-005 slice 4 (minimal seed; slice 7
# owns full image-pin consolidation, per that slice's own "Image
# consolidation" title in the handoff doc). The committed record of
# container image digests referenced by .github/workflows/*.yml's
# `image:` fields, checked by scripts/policy-lint.sh's "container:
# digests match a committed pin file" assertion (RFC-005 Part B's
# policy-lint paragraph: "container: digests match the committed pin
# file -- workflow content drift as a red check, not a review hope").
#
# One digest per line, `image@sha256:...` form -- matching the workflow's
# own `image:` value verbatim, so the check is a straight set-membership
# comparison with no parsing beyond comment/blank-line stripping (same
# convention as gates.txt/milpa-pin.txt/waivers.txt). Comments start with
# `#` as the first non-whitespace character of the line; blank lines
# ignored. Not executable (data file, no +x bit).
#
# This file intentionally holds ONLY today's one
# ghcr.io/coreyleavitt/nim digest -- the workflow's only container: image
# as of this slice, referenced by all three of unit-linux-amd64-gcc,
# property-linux-amd64-gcc, and check-readme (gates-manifest-sync,
# ruleset-sync, and policy-lint itself run on plain ubuntu-latest with no
# container: field, and are not covered by this check). Slice 7 is the
# named owner of widening this beyond a single-image seed (additional
# platform/arch images) and of collapsing merge-gate.yml's own repeated
# per-job pin comments into something generated/checked against this file
# rather than three independently-dated prose comments.
#
# Repin ritual: resolve the new digest (`podman image inspect
# ghcr.io/coreyleavitt/nim:<tag> --format '{{.Digest}}'`, as each
# workflow job's own comment already documents), update every `image:`
# line in .github/workflows/*.yml AND this file's line below, in the same
# commit -- scripts/policy-lint.sh fails loud on any mismatch between the
# two.

ghcr.io/coreyleavitt/nim@sha256:cd4708fb29d16ec4256a0bdcf8a4873b1f5a7a7200e32890ed52d5893227e780
