# RFC-006 handoff — in-house SHA-512 (FIPS 180-4)

Stage 3 (implementation) tracking doc. The RFC (`docs/rfc-006-sha512.md`,
committed at both-review-rounds state 2026-08-20) is the design authority;
this doc records slice progress, per-slice evidence, and any in-flight
findings. Standing orders: identical to RFC-001..004 (PhD-CS bar, genuine
forks escalate, wrong-spec assumptions escalate, per-slice commits after
gates pass).

## Slice status

- [x] **1a — Vectors + parser.** `tests/unit/cavp_vectors.nim` (two typed
  loaders, `loadShaByteVectors`/`loadMonteVector`, sharing hex/line
  helpers) landed via hand-written-fixture RED/GREEN cycles, then
  verified against the real vendored corpus: SHA512ShortMsg.rsp (129
  records, Len 0..1024 step 8), SHA512LongMsg.rsp (128 records),
  SHA512Monte.rsp (1 Seed, 100 COUNT/MD checkpoints) -- all spot-checked
  against literal file bytes in `tests/unit/test_sha512.nim` (now
  registered in `scripts/lib/unit-test-files.sh`). Boundary-digest
  generator `tests/vectors/gen_sha512_boundary_vectors.py` (Python
  hashlib.sha512, cross-checked against `openssl dgst -sha512`) produces
  `tests/vectors/sha512_boundary_test.json` (9 lengths: 0, 1, 111, 112,
  127, 128, 129, 239, 240), shape-sanity-checked by the same test file.
  Full `scripts/test.sh` green.
- [x] **1b — Core.** `src/sello/private/sha512.nim` (one-shot `func`
  triple + streaming register, compression core with wipe discipline,
  rolling 16-word message schedule per the slice-1b redesign below);
  ShortMsg (129/129) and LongMsg (128/128) KATs through the one-shot
  face; all 9 padding-boundary lengths in both the one-shot and
  incremental-split registers; 2/3-arg one-shot agreement;
  init-after-finish reuse; context-hygiene smoke test. Wipe-cost
  measure-record-revert: first measurement ~50% overhead (see the
  resolved slice-1b finding below), redesigned to a rolling 16-word
  schedule plus `private/ct.nim`'s new word-granular `wipe[N](var
  array[N, uint64])` overload (also covered in `test_ct.nim`), final
  measured overhead **~8%** under the identical methodology. Full
  `scripts/test.sh` green (12 unit files + 5 property suites).
- [x] **2 — Monte Carlo + properties + differential oracle.** SHAVS Monte
  chain (`test_sha512.nim`) via the three-part one-shot: all 100
  checkpoints run unconditionally by default (measured overhead ~1.5-2s
  over this file's own no-Monte baseline of ~3.4s debug-build runtime,
  nowhere near `test_properties_ristretto.nim`'s ~125s, the slowest
  standing unit file at measurement time -- so the RFC's 10-checkpoint/
  `-d:selloSha512MonteFull` fallback is not needed and was not added).
  `test_properties_sha512.nim` (determinism, incremental-vs-one-shot
  agreement over a random split point, multi-update associativity over
  two arbitrary split points), registered in
  `scripts/lib/unit-test-files.sh`. `sodiumHashSha512` wrapper
  (`crypto_hash_sha512`) added to `private/backend_sodium.nim`, plus a
  fixed-vector-and-random-sweep differential suite (one-shot and
  random-split incremental agreement) in `test_libsodium_interop.nim`.
  Both gates green: `scripts/test.sh` (12 unit files + 6 property suites)
  and `scripts/test-libsodium.sh` (same file count, `-d:selloLibsodium`,
  including the new sha512 differential suite), no vector change.
- [x] **3 — Consumer swap + dep removal.** Order followed exactly:
  challenge.nim (`challenge` now `scReduce` over `sha512(R, A, msg)`) →
  backend.nim (`derivePublic`/`signDetached` collapse onto `sha512(seed)` /
  `sha512(prefix, msg)`, caller-side `Sha2Context` vars and their wipes
  deleted, digest-output and scalar wipes plus the try/finally net
  unchanged) → test_scalar.nim (three sites: the PRNG counter-mode hash and
  both RFC 8032 test-vector-1 expansions, all now plain `sha512(x)`
  one-shots) → test_ct.nim (context-wipe coverage moved to `Sha512Context`,
  whole-object raw-byte scan via `cast[ptr UncheckedArray[byte]]` over
  `sizeof(Sha512Context)`, since its fields are private) → mutant
  re-anchor (C01/C02/B03, each verified to match the post-swap source
  exactly once before and after, F23's re-anchoring precedent followed) →
  FIRST GATE: `scripts/test.sh` and `scripts/test-libsodium.sh` both green
  (12 unit files + 6 property suites each, nimcrypto still resolvable) →
  `scripts/mutation.sh` run to confirm the re-anchored mutants still
  anchor and kill: 73/73 killed, 0 survived (355s wall clock) → dep drop
  (`milpa.kdl` drops the `nimcrypto` dep entry; `sello.nimble` drops its
  `requires "nimcrypto >= 0.7.3"` line and updates its header comment;
  `milpa fetch` on the host regenerates `nim.cfg`/`milpa.lock` down to
  zero resolved deps on a plain fetch, proptest/z3/softlink preserved at
  their pre-existing pinned commits under `--features proptest` -- see
  the in-flight finding below for how that fetch was sequenced) → SECOND
  GATE: both scripts green again (12 unit files + 6 property suites each,
  `-d:selloLibsodium` included), zero vector change -- every RFC
  8032/7748/Wycheproof/CAVP KAT and the libsodium differential suites
  (ed25519/X25519/ristretto255/sha512) produced byte-identical verdicts
  before and after the drop. `nimcrypto` no longer appears in any `import`
  statement anywhere in the tree (doc-comment/historical mentions only,
  out of this slice's scope per the handoff's own item list -- slice 5's
  job).
- [x] **4 — Adversarial depth.** Mutation catalog grew 73 → 84
  (`tests/mutation/mutants/H01`-`H12`, twelve new `src/sello/private/
  sha512.nim` mutants covering every defect class the RFC's slice-4 list
  names: Ch/Maj formula errors (H01/H02), the per-round working-variable
  shift-chain misorder (H03), a K-constant transcription bit-flip (H04),
  a BigSigma1 round-function rotation-amount off-by-one (H05), a sigma0
  message-schedule rotation-amount off-by-one (H06), an IV-word
  transcription bit-flip (H08), the 128-bit length-field hi/lo swap
  (H09), the rolling-schedule index-mask error (H10), the buffer-fill
  boundary flip at the exact fill==112 CAVP boundary edge (H11), and the
  sigma-function input swap (H12) -- plus one retired-equivalent (H07,
  see below). All twelve initial candidates were verified to match
  `sha512.nim`'s real source exactly once before the run. First full
  84-mutant run found ONE genuine survivor: H07 (the originally-drafted
  "sigma swap" mutant, which reordered the two already-computed sigma
  terms within a sum -- `w[idx] + s0 + w[(t-7) and 15] + s1` ->
  `w[idx] + s1 + w[(t-7) and 15] + s0`). Investigated per the standing
  rule rather than forced through an artificial test: this is a provably
  EQUIVALENT mutation, not a coverage gap -- uint64 addition under
  `{.push checks: off.}` is ordinary arithmetic in the abelian group
  (Z/2^64Z, +), commutative and associative for every representable
  value including wraparound, so reordering two already-computed
  summands in a sum changes nothing for ANY input, not just the ones the
  suite happens to exercise. Retired to
  `tests/mutation/mutants/equivalent/H07_compress_schedule_sigma_swap.mutant`
  with the algebraic argument recorded in its `note:` field (the F05
  retirement precedent), and replaced -- not reused -- by H12, which
  targets the RFC's actual named defect (sigma0 and sigma1 swapping which
  schedule word they READ, `w[(t-15) and 15]` vs `w[(t-2) and 15]`,
  keeping each formula's own distinct rotation constants), the same
  retire-and-replace-with-a-new-ID register F21/F22 → F23/F24 already
  established. Re-run after the fix: **84/84 killed, 0 survived, 2
  retired-equivalent (F05 pre-existing, H07 new), 553s wall clock** --
  `docs/mutation-results.md` regenerated, `scripts/mutation.sh`'s header
  comment updated with the new wall-clock figure per its own convention.
  Dudect target added (`tests/ct/ct_main.nim`, `sha512.sha512 (4-block
  compress)`: fixed 512-byte/4-block SECRET message vs. fresh random
  512-byte draw, digest folded via `backend.signDetached`'s existing
  64-byte shl/or idiom). `scripts/ct.sh` run THREE times on this host
  (busier than any previously disclosed environment in this document --
  25-26 concurrent containers throughout): Run A WARN (t=6.387), Run B
  FAIL (t=53.389), Run C FAIL (t=12.071), all three co-occurring with
  degraded readings on `ristretto.ristrettoEncode` and/or
  `` ristretto.`==` `` (both carrying pre-existing carve-out precedents
  in `docs/ct-results.md`) and/or `x25519(static) vs peer` (also a
  pre-existing shared-host artifact target on this exact host) in the
  SAME runs -- four code-unrelated targets degrading in lockstep is the
  signature of host-level noise, not four simultaneous independent
  defects. A temporary, non-shipped diagnostic (`tests/ct/
  ct_diag_sha512.nim`, built/run/deleted per the standing scratch-file
  rule, mirroring the `ct_diag_eq.nim`/`ct_diag_static.nim` precedent)
  isolated the new target from the crowded battery at 300,000
  samples/class: two REAL fixed-vs-random trials (PASS, t=2.731 and
  -2.215), two random-vs-random Control A trials (PASS, t=1.005 and
  2.713), and one fixed-vs-fixed-different Control B trial (PASS,
  t=2.597) -- five isolated trials, all clean, directly ruling out both
  content-dependence (Control B) and a construction artifact independent
  of content (Control A). **Verdict: ARTIFACT, carve-out, joining the
  existing `` ristretto.`==` ``/`x25519(static)` register** -- not a
  genuine leak; full writeup in `docs/ct-results.md`'s new "RFC-006
  slice 4" section. Disassembly verification (base Nim toolchain image,
  `objdump`/`nm` confirmed present, no `sello-dev` needed): (a) the
  compression core's per-call wipes of the 16-word rolling schedule `w`
  and the 8 working variables `v` survive `-d:release` as genuine `call`
  instructions into the word-granular `wipe[N]` overload, each of which
  disassembles to real per-`uint64` store instructions (16 + 8 = 24
  stores, matching the module doc's own count) -- not eliminated as dead
  stores; (b) the byte-generic `wipe[T]` call sites in
  `private/backend.nim` (`h`, `aBytes`, the `SecretScalar` wipes) survive
  identically, confirmed both in `derivePublic` and `signDetached`'s
  disassembly and in the wipe functions' own per-byte store loops; (c)
  the secret-bearing one-shot return paths -- inspected via the
  generated C (nimcache, `--nimcache:build/nimcache_ct`) rather than
  disassembly alone, for a direct, unambiguous read -- confirmed Nim's
  large-array-return codegen passes a hidden out-pointer (`Result`) that
  `sha512`'s `finish` call writes into DIRECTLY; at the call sites,
  `sha512(seed_p0, 32, h_1)` and `sha512(prefix_1, 32, msg_p2, ...,
  nonceHash_1)` pass `derivePublic`/`signDetached`'s own local `h`/
  `nonceHash` storage as that out-pointer -- there is exactly ONE digest
  copy on these paths (the caller's own wiped local), never an
  intermediate temporary in the one-shot's own dead stack frame. The
  RFC's recorded codegen assumption holds; **`sha512Into` was NOT
  needed** and was not added. All four gates green: `scripts/test.sh`,
  `scripts/test-libsodium.sh`, `scripts/mutation.sh` (84/84), `scripts/
  ct.sh` (investigated WARN/FAIL on the new target resolved to
  carve-out, as above; all other targets PASS, positive control FAILs
  as expected in every run).
- [ ] **5 — Docs + close-out.** CLAUDE.md, README (pitch + recipe +
  License attribution), NOTICE, script/harness comments, prompt.md
  in-place passage updates + amendment note, ristretto.nim doc example,
  CHANGELOG [0.5.0], version bumps. Also noted, out of slice-4 scope:
  `scripts/mutation.sh`'s header comment still says "46 mutants" in its
  Layout section (predates RFC-004 slice 8b's growth to 70 and the
  review round's growth to 73; slice 4 appended a new wall-clock
  sentence rather than rewriting that stale count) -- worth a pass in
  this slice alongside the other script/harness comment corrections.

## Vector provenance (recorded at vendoring, 2026-08-20)

Downloaded host-side from NIST CAVP (containers have no network):
`https://csrc.nist.gov/CSRC/media/Projects/Cryptographic-Algorithm-Validation-Program/documents/shs/shabytetestvectors.zip`
— zip sha256 `929ef80b7b3418aca026643f6f248815913b60e01741a44bba9e118067f4c9b8`.
Extracted verbatim (byte-identical to zip contents):

- `SHA512ShortMsg.rsp` — 36800 bytes, sha256 `e53a36c03609e5a3e3cc4b6e117a499db7864c23ec825c6cec99503a45f40764`
- `SHA512LongMsg.rsp` — 1687845 bytes, sha256 `b1f3f05d5c209777954d49521d7ea1349447c36a0c52849e044bc397a27dd410`
- `SHA512Monte.rsp` — 15215 bytes, sha256 `8ca78659286c2f01667a98fc7accd32fc171ae7b24ac00f1a8ce6b77770247fa`

Format note captured at vendoring: ShortMsg's `Len = 0` record carries a
placeholder `Msg = 00`; `Len` (bits) is authoritative and the parser trims
`Msg` to `Len div 8` bytes. NOTICE provenance entries land in slice 5 per
the RFC.

## Open findings / forks

**Slice 1b wipe-cost blowout (2026-08-21) -- RESOLVED same day on
Corey's direction; see resolution paragraph below.**
`private/sha512.nim` and its KAT/boundary/streaming coverage in
`test_sha512.nim` are implemented and pass the full RFC-006 slice 1b test
plan against real vectors: the empty-message KAT, the full CAVP ShortMsg
sweep (129/129) and LongMsg sweep (128/128) through the one-shot face,
all 9 padding-boundary lengths (0, 1, 111, 112, 127, 128, 129, 239, 240)
in both the one-shot and incremental-split registers (splits placed
exactly at the 0/1/111/112/127/128 thresholds), 2-arg/3-arg one-shot
agreement with the concatenated 1-arg call, init-after-finish streaming
reuse, and a whole-object wipe smoke test. `scripts/test.sh` is green in
full (12 unit files + 5 property suites) with this module compiled in.

The measure-record-revert wipe-cost step (Hygiene section's own recorded
risk point), however, measured the compression core's per-call
`ct.wipe(W)`/`ct.wipe(v)` cost at roughly **50% overhead**, not the
RFC's stated single-digit-percent expectation -- and clear past this
slice's own recorded ">25%" escalation trigger. Methodology: a throwaway
driver (session scratchpad, never committed) one-shot-hashing a fixed
128-byte input (2 `compress` calls per hash) 1,000,000 times per run,
`-d:release`, `std/times` wall-clock, compiled twice (with the core's
wipes intact, and with them stubbed out behind a temporary
`-d:selloSha512NoWipe` guard that has since been deleted from
`sha512.nim` per the RFC's own "must not survive" instruction regardless
of outcome). Two independent sample sets, 8 total timed runs, tight
non-overlapping clusters:
- With wipes: 1921-2371 ns/call (mean ~2145 ns/call across both sets).
- Without wipes: 1394-1520 ns/call (mean ~1460 ns/call across both sets).
- Ratio: roughly 1.47x, i.e. ~47-50% overhead depending on which sample
  set is used -- consistently and reproducibly far above both the RFC's
  "single-digit percent" expectation and the recorded ">25%" stop bar.

Three candidate resolutions were surfaced with the escalation (accept
the cost and amend the RFC; coarsen the wipe granularity; re-measure) --
Corey declined all three and directed a best-in-class redesign instead.

Resolution (2026-08-21, Corey's direction): attack both cost factors
rather than weakening the posture. (1) Rolling 16-word message schedule
-- W[t] depends only on W[t-2]/W[t-7]/W[t-15]/W[t-16], so the 640-byte
80-word array need never exist; secret-bearing scratch shrinks from 704
to 192 bytes (the cheapest wipe is of memory that never held the
secret). (2) A word-granular `wipe[N](var array[N, uint64])` sibling
overload in `private/ct.nim` (volatile per-uint64 stores, same barrier
and `{.noinline.}` shape; the audited byte-generic primitive untouched;
overload resolution routes word arrays there automatically), cutting the
store count another 8x. Net: 24 volatile word stores per block instead
of 704 byte stores (~30x less wipe work), per-call wipe discipline
preserved verbatim. The RFC's Hygiene section carries the same record
as an in-place dated amendment. Slice 1b's implementation resumes with
this design: refactor under the already-green KAT/boundary estate,
re-measure under the identical methodology (single-digit expectation
restored; the >25% escalation trigger stands), then gate and commit.

**Slice 3 dependency-drop fetch ordering (2026-08-21) -- RESOLVED same
session, recorded for future maintainers, not a design fork.** `milpa
fetch`'s "main"-ref resolution for the optional `proptest` dep is a
moving target, unrelated to sello: the upstream
`github.com/coreyleavitt/proptest` repository has been renamed to
`nelli` as of commits newer than the one this project's `milpa.lock` had
pinned (top-level module `src/nelli.nim`, not `src/proptest.nim`) --
confirmed by inspecting the newly-resolved commit's tree directly. A
naive drop sequence (plain `milpa fetch` to remove `nimcrypto`, followed
by `milpa fetch --features proptest` to restore proptest for the SECOND
GATE) re-resolves proptest fresh against live "main" once its lock entry
has been pruned to nothing, landing on that renamed/incompatible commit
and breaking every property-suite compile (`cannot open file: proptest`)
-- both `scripts/test.sh` (gracefully degrades to a loud SKIPPED line,
per its own documented self-skip register) and, more seriously,
`scripts/test-libsodium.sh` (`test_libsodium_interop.nim` imports
`proptest` unconditionally under `-d:selloLibsodium`, with no self-skip
of its own, so this is a hard compile failure there, not a graceful
degrade). Resolution: `milpa fetch --features proptest`, run while the
working-tree `milpa.lock` still carries proptest's PRE-EXISTING pinned
entry (i.e. run before, not after, a plain `milpa fetch` has already
pruned that entry to nothing) honors milpa's documented minimal-change/
lock-preference default and reuses the already-pinned, already-cached
commit instead of re-resolving "main" live -- `resolved N deps (frozen)`
in milpa's own output confirms no network round-trip occurred. `nimcrypto`
is still correctly dropped in the same pass, since it is no longer
declared in `milpa.kdl` at all. The committed `milpa.lock` therefore
carries the identical pre-existing proptest/z3/softlink pins it did
before this slice (same commit SHAs, same content-identity hashes) with
only the `nimcrypto` entry removed -- not a proptest upgrade bundled into
this commit. No sello-side design decision was needed; this is purely a
fetch-ordering / minimal-change-preference lesson for whoever next drops
or adds a milpa dep alongside an existing optional one pinned to a moving
branch ref.

## Resume

Slice 5 (docs + close-out) is next, via `/tdd`. Environment: `scripts/test.sh`
for gates; dudect measurement (slice 4, done) ran wherever `scripts/ct.sh`
runs, environment banner records the host -- see slice 4's own entry above
for the resolved WARN/FAIL investigation on the new `sha512` target
(ARTIFACT/carve-out, not a blocker) and the one item it surfaced for slice
5's own scope (`scripts/mutation.sh`'s stale "46 mutants" header count).
