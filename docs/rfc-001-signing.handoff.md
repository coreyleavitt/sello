# RFC-001 ed25519 signing — handoff

- **Stage:** 8 (implement) — slice grind in progress; 8/11 done; slices 9-11 unblocked (pairs (8, 9) and (10, 11) may interleave with each other; 8 precedes 9, 10 precedes 11)
- **Resume:** `/loop implement the next unimplemented RFC slice with /tdd, following the standing rules; after each slice report one progress line (e.g. "slice 4/11 done, 7 remaining"); stop when every slice is implemented`

## Slices (renumbered in round 2 — now 11)
- [x] 1 layering prep (scReduce/pointEncode → scalar.nim; challenge() extraction; clampScalar → field.nim; delete SecretKey incl. facade export, GePrecomp) — commit 6391b76, full suite green; L constant moved with scIsCanonical (entailed, not a fork)
- [x] 2 scMulAdd (ref10 port; all 27 {0,1,L−1}³ joint combos + 1000 python3-generated random vectors) — full suite green; vectors generated by checked-in tests/vectors/gen_scmuladd_vectors.py (seed 20260201) into tests/vectors/scmuladd_test.json, consumed via staticRead/parseJson like the Wycheproof vectors
- [x] 3 compile-time base table (GeCached z=1) + CT full-scan select (identity-initialized output) — commit ac2098d, full suite green; GeBaseTable[i][j] = cached((j+1)*256^i * B), built via 248 compile-time doublings + chained additions (~1s, no const-eval blocker); standing-guard test compares against vartime scalarmult via canonical point encoding (pointEncode), not raw GeCached struct equality — two different algorithms reaching the same point generally land on different, equally-valid projective (X:Y:Z:T) representatives, so raw limb comparison is a false-negative trap; cmovCached covers digit 0 (identity) and all of -8..8 with sign handling, across every table row
- [x] 4 geScalarmultBase (bit-255-clear precondition; clamped AND r-shaped vector domains; white-box digit-range test) — commit 4486596, full suite green; recodeScalarRadix16 + geScalarmultBase added to scalar.nim right after cmovCached, under their own checks-off block; consumes GeBaseTable twice (odd digit positions, x16 via four geP3Double, even digit positions) via plain geAdd since cmovCached's negation already signs the cached operand; equivalence tested against vartime scalarmult across clamped (RFC seed, 100 random, maximal 0x7F) and r-shaped (100 random reduced-mod-L, values just below L) domains, plus the white-box digit-range test over 200 scalars from both domains
- [x] 5 signing.nim types + keygen — commit 253f7b6, full suite green; **fallback used, not distinct Seed**: `Seed = distinct array[32, byte]` compiled clean with zero diagnostics but its `=destroy` demonstrably never fired for a bare local (confirmed by inspecting the generated C — no `eqdestroy` call emitted around such a local at all; it only fired when `Seed` was a field of `Keypair`, via that object's own synthesized field destructor), so RFC-001's pre-approved one-field-object fallback was applied instead, and *that* form's `=destroy` was empirically confirmed to fire for a bare local at scope exit (stderr-instrumented probe, reverted before commit). Second forced deviation: the object form can't reuse the literal `Seed(bytes)` call syntax (Nim rejects a proc/converter sharing a type's exact name — "redefinition" — where `distinct` types get that spelling for free as a built-in cast), so construction is `toSeed(bytes)` instead; both deviations are documented in `signing.nim`'s module doc comment. `derivePublic` added to new `src/sello/private/backend.nim` (seed-level, no `Keypair`/`ed25519.nim` knowledge). All four RFC 8032 §7.1 keygen vectors (TEST 1, 2, 3, TEST-1024) pass. Move-only `Keypair` (`=copy {.error.}`) verified via a subprocess `nim c` on `tests/unit/fixtures/reject_keypair_copy.nim` — `compiles()` and `nim check` both cannot see this class of error (it's raised by `injectdestructors`, a pass later than either reaches).
- [x] 6 sign core — commit 62846e2, full suite green; `backend.signDetached(seed, msg)` added right after `derivePublic` in `private/backend.nim`, under the same checks-off block: expands the seed once, derives `A` from the clamped scalar `a` directly (`pointEncode(geScalarmultBase(a))`, no second `SHA-512(seed)` call — backend stays `Keypair`-agnostic per its 2-arg contract), derives the nonce `r = scReduce(SHA-512(prefix || msg))`, `R = [r]B`, closes with the shared `scalar.challenge(R, A, msg)` (sign/verify self-consistency, unchanged since slice 1), and `S = scMulAdd(k, a, r)`. `signing.sign(kp, msg)` dispatches to it via `kp.seed.bytes` (same-module private-field access, as anticipated). RFC 8032 §7.1 TEST 1/2/3 signatures verified bit-exact against freshly re-fetched RFC text (not retyped from memory), plus sign→verify roundtrip through the existing pure `ed25519.verify` and a tampered-message negative case. TEST-1024 deliberately deferred to slice 7.
- [x] 7 sign completion — full suite green; TEST-1024 sourced by scripted extraction (new `tests/vectors/gen_test1024_vector.py`) from a verbatim paste of RFC 8032 §7.1's TEST 1024 block (`tests/vectors/rfc8032_test1024_raw.txt`, fetched from rfc-editor.org) into `tests/vectors/test1024_vector.json` — never hand-retyped; the script self-checks total extracted hex length, the message's 1023-byte length and first/last-4-byte endpoints, and cross-checks the secret/public key against the already-RFC-verified slice-5 constants, so a transcription slip fails loudly at generation time rather than silently passing; TEST-1024 sign added to `test_signing.nim` (bit-exact + roundtrip) and TEST 3 + TEST-1024 backfilled into `test_ed25519.nim`'s verify suite, both consuming the JSON via `staticRead`/`parseJson` (same pattern as the Wycheproof/scMulAdd vectors); zero-copy `sign(kp, msg: string)` (signing.nim) and `verify(sig, msg: string, pk)` (ed25519.nim) overloads added via `toOpenArrayByte`, including the empty-string edge case; facade (`src/sello.nim`) now re-exports `Seed`, `Keypair`, `toSeed`, `keypair`, `sign`, `wipe`, `public`, `seed` with a doc-comment usage example and a one-line note on the deliberate sign/verify argument-order asymmetry, backed by a new `tests/unit/test_facade.nim` regression suite; `sello.nimble`'s `test` task refactored onto a single `unitTestFiles` list + `runUnitTests(extraDefines)` helper so slice 10's `testLibsodium` task cannot drift from the main file list. Commit ae09650.
- [x] 8 secret hygiene — full suite green (83 checks incl. the new `test_ct.nim`), also re-run clean under `-d:release`; new `src/sello/private/ct.nim` provides `wipe*[T](var T)`: per-byte volatile stores (`{.emit.}`-based reimplementation of `std/volatile`'s idiom, kept a `func` since emit blocks are invisible to the effect analyzer) followed by an `asm volatile("" ::: "memory")` barrier, `{.noinline.}` so the call site itself can't be inlined away, generic over `sizeof(T)` so one primitive covers both fixed byte arrays and stack-only objects (nimcrypto's `Sha2Context`); called as `ct.wipe(...)` (qualified, matching the RFC's own notation) at every secret-scrub site: `backend.nim`'s `h`/`a`/`prefix`/`r`/`nonceHash` plus all three secret-hashing `Sha2Context`s (`derivePublic`'s `sha`; `signDetached`'s `sha` and `nonceSha` — the RFC's design section said "the two secret-hashing call sites" while sketching `sign` in the abstract, but the actual `backend.nim` that resulted from slices 5-6 has three separate SHA-512-over-secret call sites across its two functions; all three are wiped, which is what the underlying rule requires regardless of the original headcount); `x25519.nim`'s ladder scrub of `e` (the confirmed dead-store site); `signing.nim`'s `Seed` `=destroy`/`zeroizeSeed`. Also applied `{.noinline.}` to `feCMove`/`feCSwap` (`field.nim`, previously `{.inline.}`) and `cmovCached` (`scalar.nim`) — the RFC's constant-time-discipline bullet names "the feCMove/feCSwap family" as the masking helpers `{.noinline.}` is for, and those three (not anything in `ct.nim` itself) are the actual masking/select primitives in the codebase; behavior-preserving, full suite stays green. **Disassembly verification performed** (RFC-required re-check that the fix survives `-d:release`): built a standalone driver calling `x25519Base` on a non-constant secret, compiled `nim c -d:release`, objdumped the binary — `wipe` is emitted as its own non-inlined symbol (`wipe__...x25519_u42`) containing a genuine 32-iteration `movb $0x0,(%rdi)` store loop, called via `call` from the tail of `ladder` immediately before its `ret`; this is a direct, confirmed contrast to the pre-fix state (documented as already established: the naive loop was completely absent from the emitted code). No-alloc audit: grepped `backend.nim`/`x25519.nim`/`ct.nim` for `seq`/`ref`/`string`/`newSeq`/`@[` — none found outside doc comments. func/proc spot-check: `ct.wipe` and its internal `volatileStoreByte` helper are both `func` and compile clean while containing `{.emit.}` blocks, reconfirming the round-2 finding with no new caveats. Destructor smoke tests added to `test_signing.nim`'s new "Seed destructor smoke tests" suite: scope-exit wipe (raw pointer captured before the block ends, memory re-read as all-zero after `=destroy` fires) and a reassignment test (old value present via the same probe, then reassigned, then confirmed `=destroy` still fires correctly on final scope exit) — the doc comment on that suite spells out why the mid-reassignment transient zero state is not independently observable from outside the object's own default `=sink` (destroy-then-blit happens within one statement), so the test asserts what IS observable rather than overclaiming. `tests/unit/test_ct.nim` added (direct unit coverage of `ct.wipe` on both array and `Sha2Context` shapes) and wired into `sello.nimble`'s shared `unitTestFiles` list. Commit 40ba39f.
- [ ] 9 tests/ct dudect harness (interleaved classes, 4.5/10 thresholds, pinning) + docs/ct-results.md
- [ ] 10 libsodium adapter (private/backend_sodium.nim; atomic sodium_init; bidirectional interop; sello-owned Containerfile)
- [ ] 11 packaging (README, LICENSE + notices, CHANGELOG.md, doc/CLAUDE.md updates, version 0.2.0)

Ordering: 1–7 strictly sequential; pairs (8,9) and (10,11) interleave after 7; 8<9, 10<11.

## Open forks (awaiting Corey)
- none — round 2 produced no genuine forks; all findings had clear-best fixes (applied)

## Key decisions (this session)
- Slice 5: `Seed`'s RFC-specified `distinct array[32, byte]` representation is NOT used — empirically its `=destroy` never fires for a bare local (only when nested in `Keypair`), silently defeating the auto-wipe guarantee for any standalone-held `Seed` (e.g. `kp.seed()`'s return value). Switched to RFC-001's pre-approved one-field-object fallback, whose `=destroy` was confirmed (by instrumented probe) to fire for bare locals, reassignment, and nested fields alike.
- Slice 5: constructor is `toSeed(bytes)`, not `Seed(bytes)` — the object form can't take over the type's own name (Nim: "redefinition of 'Seed'"), unlike `distinct`, where `Type(x)` is a built-in cast. Both this and the representation change are recorded in `signing.nim`'s doc comment for slice 6+.
- Slice 5: move-only `Keypair` is regression-tested via a subprocess `nim c` on a checked-in negative fixture (`tests/unit/fixtures/reject_keypair_copy.nim`), not `compiles()` — `=copy {.error.}` violations are raised by `injectdestructors`, a pass that runs later than both `compiles()` and `nim check` reach, so both silently report the illegal copy as valid.
- All secret-touching code isolated in `src/sello/private/backend.nim` (seed-level backend); `ed25519.nim` stays verify-only.
- Round 2 (critical): `geScalarmultBase` precondition corrected to **bit 255 clear only** — the draft's "clamped shape (bit 254 set)" is violated by every reduced nonce r < L; r-shaped test domain + white-box digit-range test added.
- Round 2: pure backend renamed signer.nim → `private/backend.nim` (its seed-level API bypasses Keypair hygiene → private/ social contract; kills signer/signing near-collision); adapter is `private/backend_sodium.nim`.
- Round 2: `Seed = distinct array[32, byte]` with its own =destroy wipe — type-confusion guard vs PublicKey and auto-wipe for the copy `seed()` returns.
- Round 2: `Keypair` is move-only (`=copy` {.error.}); verified on 2.2.10/ORC that by-value params are borrows and reassignment destroys (wipes) the old value.
- Round 2: shared `challenge(R, A, msg)` extracted to scalar.nim, called by both verify and signDetached (sign/verify self-consistency).
- Round 2: `clampScalar` lands in **field.nim**, not scalar.nim, so x25519.nim stays a field-only consumer.
- Round 2: `keypair()` must use sysrand's in-place `urandom(dest)` overload — the seq-returning overload heap-allocates the seed.
- Round 2: old slice 6 split into sign core / sign completion → 11 slices total.
- Round 2: sello-owned minimal Containerfile for libsodium testing; `Containerfile.amox` is amoxtli infra and stays untouched.
- Round 2 de-risked empirically: emit-in-func passes the effect checker (func/proc question closed); Sha2Context verified stack-only; x25519 dead-store deletion AND the volatile-store fix confirmed by disassembly; =destroy/borrow semantics verified; checks-off does not alter VM const-eval.
- Round 1: scReduce/scIsCanonical/load3/load4/pointEncode move to scalar.nim so the backend never imports ed25519.nim.
- Round 1: base table stored as GeCached(z=1) — no GePrecomp/ge_madd port; conditional negate = swap(yPlusX,yMinusX)+negate t2d.
- Round 1: compile-time table const-eval verified empirically in-container (~1s, byte-exact); generator is contingency only.
- Round 1: `signing.nim` holds Seed/Keypair + libsodium dispatch; facade stays logic-free.
- Round 1: API = `keypair(seed)` / `keypair()` (sysrand) / `sign(kp, msg)` key-first / `wipe(var Seed)`; SecretKey deleted; sign total + deterministic.
- Round 1: nonce r named first-class secret; dudect spec hardened (interleaving, 1e6 samples, 4.5 warn / 10 fail, taskset, powersave caveat).
- `verify` remains pure-Nim even under `-d:selloLibsodium`.
- Timing harness is a separate `nimble ct` task, not part of `nimble test`.
- Slice 8: `ct.wipe[T]` (generic over `sizeof(T)`) is the one audited wipe primitive for slice 9+ to reuse as-is — call it qualified as `ct.wipe(x)` (not brought in unqualified) since `signing.nim` also exports its own concrete `wipe*(s: var Seed)` and the two are meant to stay visually distinct at call sites. Barriers live inside `ct.wipe` itself (per-`wipe`-call barrier), not as a standalone exported `barrier()` — slice 9's dudect harness, if it needs a bare barrier around a CT-select benchmark loop with no accompanying wipe, does not yet have one and should add it to `ct.nim` rather than duplicating the emit string.
- Slice 8: `{.noinline.}` now sits on all three masking/select primitives (`feCMove`, `feCSwap` in `field.nim`; `cmovCached` in `scalar.nim`) — the RFC's "masking helpers" phrase, resolved by cross-referencing its own use of "the feCMove/feCSwap family" elsewhere in the same doc. `ct.nim` does not itself define any masking primitive.

## Review ledger (stage 4)
| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
