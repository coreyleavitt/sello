# Mutation testing results (RFC-002 slice 5)

Kill-rate report for sello's curated mutation-testing catalog (`tests/mutation/mutants/`), generated by `scripts/mutation.sh` (`tests/mutation/run_mutation.py`). Regenerated wholesale on every run -- this file is a build artifact of that script, not hand-maintained.

## Summary

- **Mutants:** 73
- **Killed (test, red suite):** 73
- **Killed (compile error):** 0
- **Killed (timeout):** 0
- **Survived:** 0
- **Overall kill rate:** 100.0% (73/73)
- **Retired (confirmed-equivalent, excluded from the above):** 1
- **Wall clock:** 657s

**Gate status: clean.** Every mutant in the catalog was killed, either by the unit suite going red, by a compile error, or by exceeding the per-file timeout.

## Methodology

This is a curated, hand-written mutant catalog, not an exhaustive operator sweep: proptest's own `mutation.nim` v1 is `int -> int` only and cannot target Nim source directly, so sello builds this thin patch-based harness in its place (RFC-002 slice 5). Each mutant is an exact-string OLD -> NEW replacement (see `tests/mutation/run_mutation.py`'s module doc comment for the full format and rationale for exact-match over a fuzzy context diff), applied to a disposable scratch copy of the source tree -- the real working tree is never touched. Every mutant's OLD block is verified to occur in the real source EXACTLY ONCE, both when the catalog was written and again on every run (a 0- or multiple-occurrence match aborts the run loudly rather than silently mis-targeting).

For each mutant, the full unit suite (the same 16 files `scripts/test.sh` runs, from `scripts/lib/unit-test-files.sh`) is compiled and run against the mutated tree. A mutant is KILLED if any file fails to compile (compile-error kill -- reported separately, since a mutant that never got a chance to run proves nothing about the suite's sensitivity), if any test fails once compiled (test kill -- a real red suite), or if any one file's compile+run does not finish within the per-file timeout (timeout kill -- reported separately again, since a hang is detected and counted, not left to stall the campaign silently). A mutant SURVIVES only if every file in the suite compiles and passes within the timeout. Verdicts short-circuit on the first failing file for wall-clock reasons; a SURVIVED verdict still requires the entire suite to pass.

Scope, per RFC-002 slice 5: `field.nim`/`scalar.nim`'s highest-risk spots -- carry-chain operator swaps, shift-amount off-by-ones, boundary constants (19, 0x7FFFFF, RFC 7748's 121666, clamp masks, ...), comparison flips in `feBytesCanonical`/`scIsCanonical`, and digit-range constants in `recodeScalarRadix16`/`cmovCached`. RFC-003 slice 3 extended the catalog beyond field/scalar to the highest-stakes boundary logic elsewhere in the verify/decode surface: `challenge.nim`'s shared sign/verify hash-input ordering (a survivor there would be forgery-adjacent), `ed25519.pointDecode`'s RFC 8032 §5.1.3 reject conditions, `field.feSqrtRatioVartime`'s sqrt-ratio retry/reject branches (that primitive deleted and its two mutants replaced by RFC-004 slice 1c -- see the catalog numbering note below), `x25519.nim`'s RFC 7748 §6.1 zero-output small-order-peer check at both call sites, and `scalar.pointEncode`'s sign-bit condition (the one comparison-flip family the original S-series missed). This is quality-over-exhaustiveness curation throughout, not a claim of exhaustive operator coverage over every line of any of these files.

**Catalog numbering note:** the `field.nim`/`scalar.nim` mutant IDs skip F05 (retired to `equivalent/`, see below), and also skip F12 and F14 outright -- those two were abandoned during the original RFC-002 slice 5 authoring pass (candidate mutants that didn't survive the catalog's own curation, before ever being written to a checked-in `.mutant` file) and are not missing or lost entries; the surviving F-series simply never renumbered around the gap. F21 and F22 (originally targeting `field.feSqrtRatioVartime`'s retry/reject branches) were retired and REPLACED outright by RFC-004 slice 1c, not re-anchored: that slice deleted `feSqrtRatioVartime` after migrating `ed25519.pointDecode` onto the constant-time `feSqrtRatioM1`, so no `if` survives for either mutant's OLD-string to match. F23 and F24 replace them, targeting the CT primitive's own defect class (which candidate `feCMove` selects on `wasSquare`; the final `feAbs` normalization) instead of the deleted vartime retry/reject branches -- the F-series numbering continues rather than reusing F21/F22, so the gap is visible rather than silently overwritten.

A mutant that turns out to be behaviorally indistinguishable from the real source across its whole valid input domain (an "equivalent mutant") is retired to `tests/mutation/mutants/equivalent/` rather than left SURVIVED or forced to pass via a test that doesn't actually pin any real behavioral difference -- per the RFC's own guidance for a survivor that "reveals something deeper". Retired mutants are excluded from the active catalog this script executes (and thus from the kill-rate above) but are listed for transparency below, each with the empirical evidence for equivalence recorded in its own `note:` header field.

## Results

| id | target | description | outcome | first failing file |
|----|--------|--------------|---------|---------------------|
| B01 | private/backend.nim | signDetached: scMulAdd's two SecretScalar operands swapped, S = scMulAdd(k, a, r) [S = k*a + r] computed as scMulAdd(k, r, a) [S = k*r + a] instead -- corrupts the EdDSA signature scalar itself (S = r + k*a per RFC 8032 5.1.6 step 4), a real (non-equivalent) mutation since a (the clamped secret key scalar) and r (the per-message nonce) are different values on every real signature. | KILLED (test) | tests/unit/test_signing.nim |
| B02 | private/backend.nim | signDetached: RFC 8032 5.1.5/5.1.6's SHA-512(seed) split confused -- the clamped secret scalar `a` is built from h[32..63] (the nonce-derivation half) instead of h[0..31], and `prefix` (fed into nonce derivation) is built from h[0..31] instead of h[32..63]. Upper/lower half swap of the one-time seed expansion. | KILLED (test) | tests/unit/test_signing.nim |
| B03 | private/backend.nim | signDetached: RFC 8032 5.1.6 step 2 nonce derivation r = SHA-512(prefix \|\| msg) computed as SHA-512(msg \|\| prefix) instead -- the two `nonceSha.update` calls swapped, corrupting the per-message nonce r (and therefore R = [r]B and S = k*a + r) for every message except the degenerate empty-prefix-or-empty-msg case. | KILLED (test) | tests/unit/test_signing.nim |
| B04 | private/backend.nim | derivePublic: RFC 8032 5.1.5 clamp step dropped entirely -- the raw SHA-512(seed)[0..31] bytes are used as the secret scalar `a` unclamped (bit 254 not forced set, bits 0-2 not cleared, bit 255 not cleared), so A = [a]B is derived from the wrong scalar for every seed. | KILLED (test) | tests/unit/test_signing.nim |
| C01 | challenge.nim | challenge: hash-input order swap, SHA-512(R \|\| A \|\| msg) computed as SHA-512(A \|\| R \|\| msg) instead -- the one formula shared byte-for-byte by verify and signDetached, so this cannot be caught by a sign/verify self-consistency roundtrip (both sides call the SAME mutated function); only RFC 8032 known-answer vectors with fixed expected signature bytes pin the real R\|\|A\|\|msg order. | KILLED (test) | tests/unit/test_signing.nim |
| C02 | challenge.nim | challenge: dropped hash component, A (the public key) removed from the SHA-512(R \|\| A \|\| msg) input entirely -- the classic key-substitution-attack shape (a challenge hash that doesn't bind the signer's identity). Like C01, a sign/verify self-consistency roundtrip cannot catch this (both sides share the one mutated function); only RFC 8032/Wycheproof known-answer vectors with fixed expected bytes pin the real three-component hash. | KILLED (test) | tests/unit/test_signing.nim |
| E01 | ed25519.nim | pointDecode: RFC 8032 §5.1.3 step 4 reject condition inverted, 'sign and not feIsNonZeroVartime(root)' -> 'sign and feIsNonZeroVartime(root)' (the 'not' dropped), so the invalid root=0-with-sign-bit-set encoding is silently accepted while ordinary nonzero-root points with the sign bit set are wrongly rejected. Re-anchored on 'root' (RFC-004 slice 1c renamed the local from 'x' to 'root' between the wasSquare accept gate and the sign-bit reject check, since it now holds feSqrtRatioM1's tuple field directly) -- same reject condition, same defect class. | KILLED (test) | tests/unit/test_signing.nim |
| E02 | ed25519.nim | pointDecode: canonicity-gate entry condition inverted, 'if not feBytesCanonical(bytes)' -> 'if feBytesCanonical(bytes)' (the 'not' dropped), so decoding rejects every canonical y-coordinate encoding and (vacuously) would only proceed on non-canonical ones. | KILLED (test) | tests/unit/test_signing.nim |
| E03 | ed25519.nim | pointDecode: sign-bit reconciliation condition inverted, 'if sign: feNeg(x, x)' -> 'if not sign: feNeg(x, x)' -- negates x on the branch where the wire sign bit is CLEAR (root already matches, no negation needed) and leaves it un-negated when the sign bit is SET (a negation was needed), decoding every valid nonzero-x point to its negation. Re-cut (RFC-004 slice 1c: since feSqrtRatioM1's root is already guaranteed nonnegative, the old compare-then-maybe-negate 'feIsNegative(x) != sign' collapsed to a plain 'if sign' conditional negate -- the compare this mutant used to flip no longer exists to flip, so the same defect class (wrong-branch negation) is re-cut onto the new condition instead. | KILLED (test) | tests/unit/test_signing.nim |
| F01 | field.nim | feFromBytes: shift amount off-by-one, 'h1 shl 6' -> 'h1 shl 5' (limb-boundary shift for the 2nd input limb). | KILLED (test) | tests/unit/test_field.nim |
| F02 | field.nim | feFromBytes: boundary constant, top-limb truncation mask 8388607 (0x7FFFFF) widened to 16777215 (0xFFFFFF), letting an extra high bit of the input leak into h9 before the clamp. | KILLED (test) | tests/unit/test_signing.nim |
| F03 | field.nim | feFromBytes: reduction-by-p constant swapped, 'carry9 * 19' -> 'carry9 * 18' (p = 2^255 - 19; this is the wraparound coefficient the RFC calls out by name). | KILLED (test) | tests/unit/test_field.nim |
| F04 | field.nim | feFromBytes: shift-amount off-by-one in the final h0 carry step, 'shr 26' -> 'shr 25' (wrong limb-boundary bit count). | KILLED (test) | tests/unit/test_field.nim |
| F06 | field.nim | feToBytes: reduction constant swapped, 'h0 += 19 * q' -> 'h0 += 18 * q' (second, independent use of the p=2^255-19 coefficient in the same function). | KILLED (test) | tests/unit/test_field.nim |
| F07 | field.nim | feBytesCanonical: comparison flip, 'v < p: return true' -> 'v <= p: return true', which would accept one non-canonical encoding (a byte string equal to p in its most-significant differing position) as canonical. | KILLED (test) | tests/unit/test_field.nim |
| F08 | field.nim | feBytesCanonical: comparison flip, 'v > p: return false' -> 'v >= p: return false', rejecting some canonical encodings whose most-significant differing byte equals p's. | KILLED (test) | tests/unit/test_field.nim |
| F09 | field.nim | feAdd: carry-chain operator swap, per-limb '+' -> '-', turning field addition into field subtraction. | KILLED (test) | tests/unit/test_field.nim |
| F10 | field.nim | feSub: carry-chain operator swap, per-limb '-' -> '+', turning field subtraction into field addition. | KILLED (test) | tests/unit/test_scalar.nim |
| F11 | field.nim | feMul: reduction-by-p coefficient swapped, 'let b1_19 = 19 * b1' -> '... = 18 * b1' (the schoolbook-multiply reduction constant, not the carry-propagation tail feMul shares with feSq). | KILLED (test) | tests/unit/test_field.nim |
| F13 | field.nim | feSq: reduction-by-2p coefficient swapped, 'let a5_38 = 38 * a5' -> '... = 36 * a5' (38 = 2*19, feSq's own reduction constant distinct from feMul's). | KILLED (test) | tests/unit/test_field.nim |
| F15 | field.nim | feMul121666: RFC 7748 X25519 curve constant A24=121666 off-by-one, '121666' -> '121665'. | KILLED (test) | tests/unit/test_x25519.nim |
| F16 | field.nim | feNeg: negation removed, 'r.limbs[i] = -a.limbs[i]' -> 'r.limbs[i] = a.limbs[i]' (feNeg becomes a no-op copy). | KILLED (test) | tests/unit/test_field.nim |
| F17 | field.nim | feInvert: addition-chain iteration-count off-by-one, 'for i in 1..<5: feSq(t2, t2)' -> '1..<4', short-circuiting one squaring in the Fermat-little-theorem exponent ladder. | KILLED (test) | tests/unit/test_field.nim |
| F18 | field.nim | clampScalar: boundary constant widened, 's[0] and 248' -> 's[0] and 240' (clears bit 3 in addition to RFC 8032/7748's low 3 bits). | KILLED (test) | tests/unit/test_scalar.nim |
| F19 | field.nim | clampScalar: boundary constant changed, '(s[31] and 127) or 64' -> '... or 32', setting bit 253 instead of bit 254. | KILLED (test) | tests/unit/test_signing.nim |
| F20 | field.nim | feCMove: constant-time mask construction broken, 'let mask = -int32(b)' -> 'let mask = int32(b)' (mask becomes 0/1 instead of 0/all-ones, so the XOR-select only ever touches each limb's least significant bit). | KILLED (test) | tests/unit/test_field.nim |
| F23 | field.nim | feSqrtRatioM1: correction-select condition inverted, 'feCMove(r, rPrime, bool(uint8(flippedSignSqrt) or uint8(flippedSignSqrtI)))' -> 'feCMove(r, rPrime, not bool(uint8(flippedSignSqrt) or uint8(flippedSignSqrtI)))' -- selects the SQRT_M1-corrected candidate exactly when the ORIGINAL candidate already checked out (correctSignSqrt), and keeps the uncorrected candidate exactly when a correction was actually needed. Replaces the retired F21 (its OLD target, field.feSqrtRatioVartime, was deleted by RFC-004 slice 1c) against the CT primitive's own defect class: which candidate feCMove selects on wasSquare. Re-anchored (CT-hazard fix batch): the select combine moved from Nim's short-circuiting bool 'or' to the bitwise 'bool(uint8(a) or uint8(b))' idiom (see feSqrtRatioM1's own doc comment) to close a confirmed branch-in-disassembly finding; the OLD/NEW pair here was updated to match the new source text verbatim while preserving the identical inverted-select defect class, the same re-anchoring register as S07's precedent. | KILLED (test) | tests/unit/test_field.nim |
| F24 | field.nim | feSqrtRatioM1: final normalization inverted, 'feAbs(r)' -> 'feNeg(r, r)' -- instead of normalizing the result to its nonnegative representative (CT_ABS, RFC 9496 §4.2), unconditionally negates it, flipping the sign of every nonzero root the primitive returns. Replaces the retired F22 (its OLD target, field.feSqrtRatioVartime, was deleted by RFC-004 slice 1c) against the CT primitive's own defect class: the final feAbs normalization. | KILLED (test) | tests/unit/test_signing.nim |
| F25 | field.nim | feAbs: CT_ABS select condition inverted, 'feCMove(r, negR, feIsNegative(r))' -> 'feCMove(r, negR, not feIsNegative(r))' -- selects the negated value exactly when r is already nonnegative and leaves r unchanged (still negative) exactly when negation is actually required, defeating the nonnegative-normalization CT_ABS exists to guarantee (RFC 9496 §4.2, consumed by feSqrtRatioM1/ristrettoEncode/the map). RFC-004 slice 8b. | KILLED (test) | tests/unit/test_field.nim |
| F26 | field.nim | feEqualCT: bitwise or-accumulate changed to and-accumulate, 'diff = diff or (ab[i] xor bb[i])' -> 'diff = diff and (ab[i] xor bb[i])' -- a single differing byte no longer forces diff nonzero once any earlier byte pair's xor happened to and to zero with the running value, so most unequal field elements would spuriously compare equal. RFC-004 slice 8b. | KILLED (test) | tests/unit/test_field.nim |
| F27 | field.nim | feBytesCanonicalCT: same or-to-and accumulate corruption as F26's feEqualCT mutant, in the CT canonicity check ristrettoDecode's accept gate depends on, 'diff = diff or (roundTrip[i] xor bytes[i])' -> 'diff = diff and (roundTrip[i] xor bytes[i])' -- most non-canonical (>= p) encodings would spuriously round-trip as canonical. RFC-004 slice 8b. | KILLED (test) | tests/unit/test_field.nim |
| F28 | field.nim | feSqrtRatioM1: correct_sign_sqrt's own check target flipped, 'feEqualCT(check, u)' -> 'feEqualCT(check, negU)' -- duplicates flipped_sign_sqrt's comparison exactly, losing the primitive's ability to recognize the directly-correct candidate root (RFC 9496 §4.2's first of three checks). One of the "three checks flipped individually" mutants the RFC's slice 8b calls for, distinct from F23 (the correction-select combine) and F24 (the final feAbs normalization). | KILLED (test) | tests/unit/test_signing.nim |
| F29 | field.nim | feSqrtRatioM1: flipped_sign_sqrt's own check target flipped, 'feEqualCT(check, negU)' -> 'feEqualCT(check, u)' -- duplicates correct_sign_sqrt's comparison exactly, losing the SQRT_M1-correction branch's own trigger condition (RFC 9496 §4.2's second of three checks). One of the "three checks flipped individually" mutants the RFC's slice 8b calls for, distinct from F23/F24. | KILLED (test) | tests/unit/test_field.nim |
| F30 | field.nim | feSqrtRatioM1: flipped_sign_sqrt_i's own check target flipped, 'feEqualCT(check, negUSqrtM1)' -> 'feEqualCT(check, u)' -- loses the third check's own defining condition (RFC 9496 §4.2's flipped_sign_sqrt_i, v*r^2 == -u*SQRT_M1) entirely, the branch the false-branch root's own defining-equation test (root^2 == SQRT_M1*u/v on known non-squares) exists to catch. One of the "three checks flipped individually" mutants the RFC's slice 8b calls for, distinct from F23/F24. | KILLED (test) | tests/unit/test_field.nim |
| R01 | ristretto.nim | ristrettoDecode: canonicity reject term inverted, 'if (not canonical) or ...' -> 'if canonical or ...' -- rejects every canonical encoding and accepts non-canonical (>= p) ones instead, the opposite of RFC 9496 §4.3.1 step 1. RFC-004 slice 8b. | KILLED (test) | tests/unit/test_facade.nim |
| R02 | ristretto.nim | ristrettoDecode: negative-s reject term inverted, 'sNegative' -> '(not sNegative)' in the verdict combine -- rejects nonnegative s and accepts negative s instead, the opposite of RFC 9496 §4.3.1 step 2. RFC-004 slice 8b. | KILLED (test) | tests/unit/test_facade.nim |
| R03 | ristretto.nim | ristrettoDecode: was_square reject term inverted, '(not wasSquare)' -> 'wasSquare' in the verdict combine -- rejects every square case (the intended accept path) and accepts every non-square case instead. RFC-004 slice 8b. | KILLED (test) | tests/unit/test_facade.nim |
| R04 | ristretto.nim | ristrettoDecode: negative-t reject term inverted, 'tNegative' -> '(not tNegative)' in the verdict combine -- rejects nonnegative t and accepts negative t instead, the opposite of RFC 9496 §4.3.1 step 4. RFC-004 slice 8b. | KILLED (test) | tests/unit/test_facade.nim |
| R05 | ristretto.nim | ristrettoDecode: y=0 reject term inverted, 'yZero' -> '(not yZero)' in the verdict combine -- rejects every nonzero-y point (essentially everything) and accepts only y=0, the opposite of RFC 9496 §4.3.1's final reject condition. RFC-004 slice 8b. | KILLED (test) | tests/unit/test_facade.nim |
| R06 | ristretto.nim | ristrettoEncode: sqrt(-1) rotation trigger inverted, 'let rotate = feIsNegative(tZinv)' -> 'let rotate = not feIsNegative(tZinv)' -- rotates x/y/den_inv exactly when RFC 9496 §4.3.2 says not to, and leaves them unrotated exactly when the rotation is required. RFC-004 slice 8b's "encode's rotation ... condition flip" target. Re-anchored (round-2 review finding R2-1): ristrettoEncode's body is now wrapped in a try/finally that unconditionally wipes its own named Fe locals before return (the sink-ephemeral ristrettoScalarmult overload routes a secret DH product through it), shifting this line's indentation from 2 spaces to 4 -- the OLD/NEW pair was updated to match the new source text verbatim, the same re-anchoring register as S07/F23's precedent, with no change to the mutant's targeted defect class. | KILLED (test) | tests/unit/test_facade.nim |
| R07 | ristretto.nim | ristrettoEncode: final sign-correction select inverted, 'feCMove(y, negY, feIsNegative(xZinv))' -> 'feCMove(y, negY, not feIsNegative(xZinv))' -- negates y exactly when x*z_inv is already nonnegative and leaves it alone exactly when negation is required, producing the wrong-sign encoding for every non-degenerate point. RFC-004 slice 8b's "encode's ... negation condition flip" target. Re-anchored (round-2 review finding R2-1): ristrettoEncode's body is now wrapped in a try/finally that unconditionally wipes its own named Fe locals before return (the sink-ephemeral ristrettoScalarmult overload routes a secret DH product through it), shifting this line's indentation from 2 spaces to 4 -- the OLD/NEW pair was updated to match the new source text verbatim, the same re-anchoring register as S07/F23's precedent, with no change to the mutant's targeted defect class. | KILLED (test) | tests/unit/test_facade.nim |
| R08 | ristretto.nim | RistrettoPoint `==`: top-level bitwise combine changed from or to and, 'bool(uint8(eq1) or uint8(eq2))' -> 'bool(uint8(eq1) and uint8(eq2))' -- RFC 9496 §4.3.3's quotient equality requires EITHER cross-product check to hold (x1*y2 == y1*x2 OR y1*y2 == x1*x2); requiring BOTH rejects most genuinely-equal representative pairs of the same element. RFC-004 slice 8b's named "equality's or->and" mutant, also anchoring the bitwise-combine shape the module doc calls out as the one place a short-circuit rewrite would break the OLD-string match. | KILLED (test) | tests/unit/test_facade.nim |
| R09 | ristretto.nim | ristrettoMap: the s CT_SELECT condition inverted, 'feCMove(s, negAbsSt, not wasSquare)' -> 'feCMove(s, negAbsSt, wasSquare)' -- takes the negated-absolute-value branch exactly when was_square holds (the case RFC 9496 §4.3.4 says to keep the candidate root unchanged) and vice versa. RFC-004 slice 8b's "the map's was_square select flip" target. | KILLED (test) | tests/unit/test_facade.nim |
| R10 | ristretto.nim | ristrettoMap: the c CT_SELECT condition inverted, 'feCMove(c, r, not wasSquare)' -> 'feCMove(c, r, wasSquare)' -- selects r exactly when was_square holds (the case RFC 9496 §4.3.4 says c should stay -1), corrupting the numerator N's sign term for every square case. Second instance of RFC-004 slice 8b's "the map's was_square select flip" target, alongside R09. | KILLED (test) | tests/unit/test_ristretto.nim |
| R11 | ristretto.nim | geCachedNegate: yPlusX/yMinusX swap dropped on the yPlusX component, 'result.yPlusX = q.yMinusX' -> 'result.yPlusX = q.yPlusX' -- negating a point via GeCached requires swapping which sum (y+x vs y-x) each field holds (negating x swaps which is which); this leaves yPlusX unswapped (a no-op on that one component) while yMinusX/t2d/z still change correctly, so every `-` result carries the WRONG x for its correct y -- a genuinely different (wrong) point, not the input's negation. Backs both the binary and unary `-` operators (RFC-004 slice 8b's mutation-catalog gap: geCachedNegate itself had no direct mutant, only callers). | KILLED (test) | tests/unit/test_facade.nim |
| R12 | ristretto.nim | ristrettoScalarmult(sink RistrettoEphemeralSecret, p): RFC 9496 §4.3.2 identity-encoding zero-check inverted, 'if acc == 0' -> 'if acc != 0' -- the round-4 finding R3-1 degenerate-peer rejection (a RistrettoIdentity peer, or k == 0 mod L, both encode to 32 zero bytes) is inverted, so a genuine degenerate DH output is returned as Some(RistrettoShared) instead of rejected, and every real nonzero shared secret is dropped as None. The x25519 sibling zero-check mutants (X01/X02) are this finding's precedent. | KILLED (test) | tests/unit/test_facade.nim |
| S01 | scalar.nim | geP3ToCached: sign flip, 't2d = 2*d*T' computed via feSub instead of feAdd. | KILLED (test) | tests/unit/test_scalar.nim |
| S02 | scalar.nim | geP2Dbl: sign flip in point doubling, 'X+Y' computed via feSub instead of feAdd. | KILLED (test) | tests/unit/test_scalar.nim |
| S03 | scalar.nim | geP2Dbl: sign flip in point doubling, 'Y^2 - X^2' computed via feAdd instead of feSub. | KILLED (test) | tests/unit/test_scalar.nim |
| S04 | scalar.nim | geAdd: sign flip, '2*Z*Zq' (D+D) computed via feSub instead of feAdd, zeroing the term instead of doubling it. | KILLED (test) | tests/unit/test_scalar.nim |
| S05 | scalar.nim | geAdd: sign flip in the Edwards addition formula's X output, computed via feAdd instead of feSub. | KILLED (test) | tests/unit/test_scalar.nim |
| S06 | scalar.nim | scalarmultVartime: boundary constant narrowed, nibble mask 'and 0xF' -> 'and 0x7', dropping the window's top bit. | KILLED (test) | tests/unit/test_scalar.nim |
| S07 | scalar.nim | scalarmultVartime: loop-bound off-by-one, 'countdown(63, 0)' -> 'countdown(62, 0)', skipping the scalar's top nibble entirely. Re-anchored with a second context line (RFC-004 slice 8b): RFC-004 slice 7a's geScalarmultCT independently introduced its own 'for i in countdown(63, 0):' loop (4-space indent) whose tail is a substring match of this mutant's original 2-space-indent OLD block under plain string containment, so the OLD block was widened to the loop body's first line (scalarmultVartime-only text) to restore a unique match -- the same re-anchoring register as the round-3 batch A drill, not a semantic change to what this mutant targets. | KILLED (test) | tests/unit/test_scalar.nim |
| S08 | scalar.nim | cmovCached: digit-range constant narrowed, 'for i in 1..8' -> '1..7', dropping the table's 8th-multiple entry from the constant-time scan. | KILLED (test) | tests/unit/test_scalar.nim |
| S09 | scalar.nim | cmovCached: operator swap in the branchless absolute-value formula, '(digit xor signMask) - signMask' -> '... + signMask'. | KILLED (test) | tests/unit/test_scalar.nim |
| S10 | scalar.nim | recodeScalarRadix16: digit-range constant narrowed, low-nibble mask 'and 0xF' -> 'and 0x7' for the even-indexed digits. | KILLED (test) | tests/unit/test_scalar.nim |
| S11 | scalar.nim | recodeScalarRadix16: digit-range constant narrowed, high-nibble mask 'and 0xF' -> 'and 0x7' for the odd-indexed digits. | KILLED (test) | tests/unit/test_scalar.nim |
| S12 | scalar.nim | recodeScalarRadix16: digit-range constant changed, carry-formation offset '(result[i] + 8) shr 4' -> '(result[i] + 9) shr 4', shifting the signed-digit recentering point. | KILLED (test) | tests/unit/test_scalar.nim |
| S13 | scalar.nim | recodeScalarRadix16: shift-amount off-by-one, 'result[i] -= carry shl 4' -> 'carry shl 3', halving the effective carry contribution removed from each digit. | KILLED (test) | tests/unit/test_scalar.nim |
| S14 | scalar.nim | geScalarmultBase: iteration-count off-by-one, 'for _ in 0 ..< 4: acc = geP3Double(acc)' -> '0 ..< 3', scaling the accumulator by 8 instead of 16 between the odd- and even-digit passes. | KILLED (test) | tests/unit/test_scalar.nim |
| S15 | scalar.nim | scIsCanonical: comparison flip, 's[i] < L[i]: return true' -> 's[i] <= L[i]: return true', accepting one non-canonical (>= L) scalar encoding. | KILLED (test) | tests/unit/test_scalar.nim |
| S16 | scalar.nim | scIsCanonical: comparison flip, 's[i] > L[i]: return false' -> 's[i] >= L[i]: return false', rejecting some canonical (< L) scalar encodings. | KILLED (test) | tests/unit/test_scalar.nim |
| S17 | scalar.nim | L (subgroup order) constant corrupted: the top byte of the 32-byte little-endian encoding changed from 0x10 to 0x20, doubling the encoded modulus. | KILLED (test) | tests/unit/test_ristretto.nim |
| S18 | scalar.nim | scReduce: shift-amount off-by-one in the top-limb (s23) extraction, 'load4(s, 60) shr 3' -> 'shr 2'. | KILLED (test) | tests/unit/test_signing.nim |
| S19 | scalar.nim | scMulAdd: shift-amount off-by-one in the 'a' operand's top-limb (a11) extraction, 'load4(a, 28) shr 7' -> 'shr 6'. | KILLED (test) | tests/unit/test_scalar.nim |
| S20 | scalar.nim | pointEncode: sign-bit condition flip, 'if feIsNegative(x)' -> 'if not feIsNegative(x)' -- the one comparison-flip family the original S-series missed (RFC-003 slice 3): sets the wire sign bit on every non-negative x and clears it on every negative x, the exact opposite of RFC 8032 §5.1.2's encoding rule. | KILLED (test) | tests/unit/test_scalar.nim |
| S21 | scalar.nim | geScalarmultCT: per-digit doubling count off-by-one, 'for _ in 0 ..< 4: acc = geP3Double(acc)' -> '0 ..< 3', scaling the accumulator by 8 instead of 16 at each of the 64 digit positions in the uniform interleaved ladder -- computes the wrong multiple for every nonzero scalar. The geScalarmultCT-specific sibling of S14's geScalarmultBase mutant (RFC-004 slice 8b). | KILLED (test) | tests/unit/test_scalar.nim |
| S22 | scalar.nim | geScalarmultCT: runtime [1P..8P] table-build step selects the wrong cached operand, 'geAdd(step0, pts[i - 1], table[0])' -> 'geAdd(step0, pts[i - 1], table[i - 1])' -- doubles pts[i-1] against itself instead of adding P via table[0], so the table holds powers of two times P (P, 2P, 4P, 8P, 16P, ...) instead of consecutive multiples (P, 2P, 3P, ..., 8P), corrupting every digit lookup downstream. RFC-004 slice 8b's "table-select index flip" target. | KILLED (test) | tests/unit/test_scalar.nim |
| S23 | scalar.nim | geScalarmultCT: accumulator declared without its identity seed, 'var acc: GeP3 = geP3Identity()' -> 'var acc: GeP3' -- leaves acc as Nim's implicit all-zero GeP3 (x=0, y=0, z=0, t=0) instead of the group identity (0,1,1,0) before the ladder's first iteration. The same defect class RFC-004 slice 7a's own side-quest caught in scalarmultVartime's s=0 path (a pre-existing bug fixed in that slice), reproduced here on the CT variable-base ladder for every call. RFC-004 slice 8b. | KILLED (test) | tests/unit/test_scalar.nim |
| S24 | scalar.nim | geScalarmultCT: per-iteration table lookup negates the signed radix-16 digit before selection, 'cmovCached(u, table, digits[i])' -> 'cmovCached(u, table, -digits[i])' -- computes [-s]p (the group inverse of the intended multiple) for every nonzero scalar. Targets geScalarmultCT's own call site specifically (distinct from S08/S09, which mutate cmovCached's shared internals and are already exercised by both geScalarmultBase and geScalarmultCT). RFC-004 slice 8b's "digit-sign flip" target. | KILLED (test) | tests/unit/test_scalar.nim |
| S25 | scalar.nim | scIsCanonicalCT: verdict flip, 'borrow == 1' -> 'borrow == 0', inverting the CT canonicity check (finding 2's CT counterpart to scIsCanonical -- accepts every non-canonical scalar and rejects every canonical one). | KILLED (test) | tests/unit/test_scalar.nim |
| X01 | x25519.nim | x25519(X25519StaticSecret, peer): RFC 7748 §6.1 zero-output small-order-peer check inverted, 'if acc == 0' -> 'if acc != 0', so an attacker-supplied small-order peer point (all-zero DH output) is returned as Some(X25519Shared) instead of rejected, and every genuine nonzero shared secret is dropped as None. | KILLED (test) | tests/unit/test_facade.nim |
| X02 | x25519.nim | x25519(sink X25519EphemeralSecret, peer): the same RFC 7748 §6.1 zero-output small-order-peer check inverted at the ephemeral-secret call site, 'if acc == 0' -> 'if acc != 0', independently of X01's static-secret overload (the two are separate functions sharing no code past the ladder call). | KILLED (test) | tests/unit/test_facade.nim |

## Retired (equivalent) mutants

Not executed by this run and not counted in the summary above -- see the methodology section.

### F05 (field.nim)

feToBytes: reduction constant swapped in the canonicalization quotient estimate, '19 * cast[int64](h9)' -> '18 * ...'.

Verified empirically (not merely argued) to be a genuinely equivalent mutant over the WHOLE documented Fe invariant domain (field.nim's own per-limb range doc comment: h0/h2/h4/h6/h8 in [0, 2^26), h1/h3/h5/h7/h9 in [0, 2^25)) -- ~206,000 cases with zero divergence: a fine-grained sweep of the near-p/near-2^255 boundary region (+/-2000 around p, the exact region test_properties_field.nim's own near-p boundary stress already targets), a directed search at every arithmetic position where a 1-unit difference in the seed estimate could tip a later 'shr 26'/'shr 25' carry decision (h9 at its extremes crossed with h0 at every 'boundary - k' offset for k in -40..40, both near 2^26 and near 0), and 200,000 uniformly random samples over the full declared-valid limb domain. This mutated line only ever produces a SEED for the quotient estimate q -- every one of the 9 refinement steps that follows re-derives q from the actual (unmutated) h0..h9 values via successive 'shr 25/26' carries, and the domain sello's Fe invariant restricts feToBytes's real callers to (the value represented is always within one wraparound of canonical, i.e. q in {0, 1}) turns out to leave enough slack in that refinement chain to fully absorb a 1-unit seed error, matching the design intent of ref10's original quotient-estimation technique (an exact seed was never actually required, only a close-enough one). This is NOT the same situation as "the mutant is equivalent because the current code is buggy" (RFC-002 slice 5's escalation clause) -- the current code (with 19) is correct and RFC/Wycheproof-clean; this specific single line is simply provably redundant given the other 19-application four lines away (feMul's 'h0 += 19 * q', mutant F06, which WAS killed) and the refinement loop's own tolerance. Reported as a finding per the slice's own guidance ("if a survivor reveals something deeper ... report it as a finding rather than forcing a fake test") rather than forced into an artificial assertion that would not actually pin any real behavioral difference. See docs/mutation-results.md's "Retired (equivalent) mutants" section.

